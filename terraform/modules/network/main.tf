# Welcome to the network module
# This module provides the network base (VPC, subnet, internet gateway, security group) for the clusters and instances that will be deployed
# Security is purposefully lax, as these are intended to be temporary test environments